import React from "react";

const PowerSettingsNew = () => <i className="material-icons">power_settings_new</i>;

const Settings = () => <i className="material-icons">settings</i>;

const DashboardIcon = () => <i className="material-icons">dashboard</i>;

const Build = () => <i className="material-icons">build</i>;

const Close = () => <i className="material-icons">close</i>;

const Cached = () => <i className="material-icons">cached</i>;

const Search = () => <i className="material-icons">search</i>;

const Refresh = () => <i className="material-icons">refresh</i>;

const Swap = () => <i className="material-icons">swap_horiz</i>;

export {
  PowerSettingsNew,
  Settings,
  DashboardIcon,
  Build,
  Close,
  Cached,
  Search,
  Refresh,
  Swap,
};
